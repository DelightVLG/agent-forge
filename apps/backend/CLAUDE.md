# Backend App Rules

> This file is seeded by `context-collector` during `/init-project`. Until then, it only contains defaults.

## Stack
<filled by context-collector>

## Commands
- Install: `pnpm --filter backend install`
- Dev: `pnpm --filter backend dev`
- Test: `pnpm --filter backend test`
- Lint: `pnpm --filter backend lint`

## Skills
See `.claude/skills/backend/` and `.claude/skills/common/`.

## Boundaries
Backend code stays in this folder. Shared types go in `packages/shared` (create when needed).
