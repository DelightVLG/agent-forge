# Project: <not initialized>

> Run `/init-project` (or `./.scripts/init-project.sh`) to populate this file.
> Until then, agents will refuse to plan or code.

<!--
  After /init-project this file becomes the single source of truth for the
  whole project: identity, features, stack, architecture, deploy, conventions.

  KEEP IT ALIVE:
  - When a feature is shipped → flip its status in the Features section.
  - When a new feature is scoped → project-manager adds it under the right bucket.
  - When the stack changes (new library, new service) → update the relevant
    section the same PR.
  - When a hard decision is made → link to the ADR in .agent-memory/decisions/.

  If it's not written here, it's forgotten.
-->
