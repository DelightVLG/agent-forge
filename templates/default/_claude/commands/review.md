---
description: Run codex-reviewer on the current branch diff.
---

Invoke the `codex-reviewer` subagent on the current branch. It will diff against
main, run `codex exec`, and report verdict + findings. Append the review to the
current task file.
