---
description: Show all active tasks and their status.
---

List all files in `.agent-memory/tasks/`. For each, extract and show: ID, title,
status, app, issue link, branch. Group by status. Also run
`git branch --show-current` and `gh pr list --author @me` so I can see what's in
flight.
